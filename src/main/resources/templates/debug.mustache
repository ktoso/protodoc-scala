<html>
<head>
    <title>ProtoDoc for: {{data.packageName}}.{{data.fieldName}}</title>
    {{> styles_css}}
    {{> message_styles_css}}
</head>
<body>
<h1 class="message-header">
    <img src="images/message-big.png" alt="msg"/> {{data.messageName}}
</h1>
<span class="top-level-comment">{{& comment}}</span>

<!--todo add package pages or something, get rid of this if! vvvvv -->
{{#data.isInnerMessage}}
<h2>
    defined in message: <a href="{{data.packageName}}.html">{{data.packageName}}</a>
</h2>
{{/data.isInnerMessage}}
{{^data.isInnerMessage}}
<h2>
    defined in package: {{data.packageName}}
</h2>
{{/data.isInnerMessage}}
<!--todo add package pages or something, get rid of this if! ^^^^^ -->

<h2 class="fields-header sub-header">Fields:</h2>
<ul class="fields">
    {{#data.fields}}
    <li>
        <strong>{{data.fieldName}}</strong><br/>
        <span class="comment">{{& comment}}</span><br/>

        <dl>
            {{#data.defaultValue}}
            <dt>Default value:</dt>
            <dd>{{data.defaultValue}}</dd>
            {{/data.defaultValue}}

            <dt>Modifier:</dt>
            <dd>{{data.modifier}}</dd>

            <dt>Tag:</dt>
            <dd>{{data.tag}}</dd>

            <dt>Defined as:</dt>
            <dd>{{data.protoTypeName}}</dd>

            <dt>Mapped to:</dt>
            <dd>{{data.scalaTypeName}}</dd>
        </dl>
    </li>
    {{/data.fields}}
    {{^data.fields}}
    <li>
        This message defines no fields.
    </li>
    {{/data.fields}}
</ul>

<h2 class="inner-enums-header sub-header">Inner Enums:</h2>
<ul class="inner-enums">
    {{#data.enums}}
    <li>
        <strong>
            <a class="msg-link" href="{{data.packageName}}.{{data.typeName}}.html">
                {{data.typeName}}
            </a>
        </strong><br/>
        <span class="comment">{{& comment}}</span>
        Defines values:
        <table>
            <thead>
            <tr>
                <td>Value</td>
                <td>TAG</td>
                <td>Comment</td>
            </tr>
            </thead>
            <tbody>
            {{#data.values}}
            <tr>
                <td class="code">{{data.valueName}}</td>
                <td>{{data.tag}}</td>
                <td>{{& comment}}</td>
            </tr>
            {{/data.values}}
            </tbody>
        </table>
    </li>
    {{/data.enums}}
    {{^data.enums}}
    <li>
        This message defines no enums.
    </li>
    {{/data.enums}}
</ul>

<h2 class="inner-msgs-header sub-header">Inner messages:</h2>
<ul class="inner-msgs">
    {{#data.innerMessages}}
    <li>
        <strong>
            <a class="msg-link" href="{{data.packageName}}.{{data.messageName}}.html">
                {{data.messageName}}
            </a>
        </strong>
        <span class="comment">{{& comment}}</span>
    </li>
    {{/data.innerMessages}}
    {{^data.innerMessages}}
    <li>
        This message defines no inner messages.
    </li>
    {{/data.innerMessages}}
</ul>

{{> fork_me }}
<div class="footer-placemaker"/>
</body>
</html>